# ── Backend (server/) — set these on Railway ──
DATABASE_URL=postgresql://user:password@host:port/dbname
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
PORT=3001
# Supabase admin access (Project Settings → API). The service_role key bypasses
# RLS — server-side only, never expose it to the client.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJxxxxxxxx

# ── Frontend (client/) — set on Vercel once the API is wired (Phase 2) ──
# VITE_API_URL=https://your-railway-app.up.railway.app
